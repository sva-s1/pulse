metadata_details:
  purpose: Parser for ISC BIND DNS logs using the OCSF mapping to normalise events into
    the AI-SIEM schema.
  datasource_vendor: isc
  dataSource: bind
  format: OCSF
  ingestion_method: unknown
  dependency_summary: Requires ingestion of the original ISC BIND DNS query logs.
  performance_impact: "Minimal – this parser processes DNS query and response events."
  tags: isc, bind, dns, query, network, ocsf, parser
  version: latest