metadata_details:
  purpose: Parser for Google Cloud DNS logs using JSON parsing to normalise events
    into the AI-SIEM schema.
  datasource_vendor: google
  dataSource: cloud_dns
  format: JSON
  ingestion_method: unknown
  dependency_summary: Requires ingestion of Google Cloud DNS query logs.
  performance_impact: "Minimal – this parser processes JSON formatted DNS logs."
  tags: google, cloud, dns, logs, json, parser, gcp
  author: Marek Kratky
  version: latest