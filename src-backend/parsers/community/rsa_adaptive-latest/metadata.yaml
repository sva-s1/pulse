metadata_details:
  purpose: Parser for RSA Adaptive logs using the OCSF mapping to normalise events into
    the AI-SIEM schema.
  datasource_vendor: rsa
  dataSource: adaptive
  format: OCSF
  ingestion_method: unknown
  dependency_summary: Requires ingestion of the original RSA Adaptive authentication logs.
  performance_impact: "Minimal – this parser processes adaptive authentication events."
  tags: rsa, adaptive, authentication, risk, security, ocsf, parser
  version: latest