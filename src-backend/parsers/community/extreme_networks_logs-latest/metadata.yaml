metadata_details:
  purpose: Parser for Extreme Networks logs using JSON parsing to normalise events
    into the AI-SIEM schema.
  datasource_vendor: extreme
  dataSource: networks
  format: JSON
  ingestion_method: unknown
  dependency_summary: Requires ingestion of Extreme Networks device logs.
  performance_impact: "Minimal – this parser processes JSON formatted network logs."
  tags: extreme, networks, logs, json, parser, networking
  author: Kenny Cho
  version: latest